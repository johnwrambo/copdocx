# Leaflet 1.9.4

Local copy of [Leaflet](https://leafletjs.com/) so Case / Target sheet maps load without the unpkg CDN. Tiles (OSM / Esri) still need a network connection; if tiles fail, pins stay on a plain basemap.
